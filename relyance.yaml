# Relyance Compliance Inspector Configuration

# Product and Repository Metadata
# Purpose: Useful to slice/dice the Data Inventory and Data Map in Relyance
repository: das


# Services and Code Relationship
# Purpose: Configuration used to ascertain relationship between different logical components and code.
# Note: Directory paths are relative to the repository's root directory. 
# Note: Need a separate entry for each component. 
services:
  - service: earthranger-das-repo
    product:
      - EarthRanger
    source:
      - ./


# Source Directories to Exclude
# Purpose: Configuration is used to exclude directories from analysis.
# TODO: Add directory paths are relative to the repository's root directory that you want to exclude from the scan. This is optional.
# exclude:
#   - 
