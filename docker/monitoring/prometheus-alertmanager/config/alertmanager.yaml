global:
  slack_api_url: ${ALERTS_SLACK_URL}

route:
  group_by: [alertname, datacenter, app]
  receiver: das-slack

receivers:
- name: 'das-slack'
  slack_configs:
  - api_url: ${ALERTS_SLACK_URL}
    channel: ${ALERTS_SLACK_CHANNEL}
    send_resolved: true

