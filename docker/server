docker stop dasserver && docker rm dasserver
docker build -t das/server -f docker/API/Dockerfile . 
docker run -d -v "$(pwd)/das":/var/www/das --link redis:redis --link postgis:postgis --name dasserver das/server
IP="$(docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' dasserver)"
echo $IP