version: "3.2"

services:
  nginx:
    build:
      context: ./docker/Nginx
      
  earthranger-api:
    image: gcr.io/padas-app/circleci/earthranger-api
    build:
      context: ./docker/

  postgis:
    build:
      context: docker/PostGIS

