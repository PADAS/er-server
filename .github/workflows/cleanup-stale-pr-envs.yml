name: Cleanup Stale PR Environments
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  cleanup:
    uses: PADAS/serca-pipelines/.github/workflows/pipeline-cleanup-stale-pr-envs.yml@main