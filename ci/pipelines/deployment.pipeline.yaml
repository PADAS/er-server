resources:
- name: git-deployment
  type: versioned-git-resource
  source:
    uri: git@github.com:PADAS/das.git
    branch: ((server-branch-name))
    paths:
      - deployment/*
    private_key: ((das-github))

resource_types:
- name: versioned-git-resource
  type: docker-image
  source:
    email: ((gcr-io-email))
    username: ((gcr-io-username))
    password: ((ss-infrastructure-container-registry-pull))
    repository: gcr.io/ss-infrastructure/concourse/versioned-git-resource

jobs:
- name: deploy-to-k8s
  plan:
  - aggregate:
    - get: git-deployment
  - aggregate:
      - task: ensure-disks-exist
        config:
          platform: linux
          image_resource:
            type: docker-image
            source: {
              repository: gcr.io/ss-infrastructure-public/platform/tools,
              tag: ((vp-tools-container-version)),
            }
          inputs:
          - name: git-deployment
          params: {
            PROJECT: padas-app,
            SERVICE_ACCOUNT_KEY: ((padas-app-boshprovisioner)),
            USERNAME: ((creator)),
            ELASTIC_URL: ((elastic-url)),
            BUILD_PIPELINE_NAME: ((pipeline-name))
          }
          run:
            path: /vulcan-platform-tools/run/run.with.gcloud.service.account.sh
            args:
            - vcloud
            - disks
            - create
            - git-deployment/deployment/storage/default.disks.csv
            - --prefix=((cluster-name))
            - --zone=((gcp-compute-zone))
            - --iaas=gcp
            - --workspace=((iaas-workspace))
            - --type=ssd
      - task: ensure-cluster-exists
        config:
          platform: linux
          image_resource:
            type: docker-image
            source: {
              repository: gcr.io/ss-infrastructure-public/platform/tools,
              tag: ((vp-tools-container-version)),
            }
          inputs:
          - name: git-deployment
          params: {
            PROJECT: padas-app,
            SERVICE_ACCOUNT_KEY: ((padas-app-cluster-admin)),
            USERNAME: ((creator)),
            VAULT_TOKEN: ((vault-automation-token)),
            VAULT_ADDR: ((vault-address)),
            ELASTIC_URL: ((elastic-url))
          }
          run:
            path: /vulcan-platform-tools/run/run.with.gcloud.service.account.sh
            args:
            - vcloud
            - clusters
            - create
            - git-deployment/((cluster-spec))
            - --name=((cluster-name))
            - --zone=((gcp-compute-zone))
            - --iaas=gcp
            - --workspace=((iaas-workspace))
  - do:
      - task: deploy
        config:
          platform: linux
          image_resource:
            type: docker-image
            source: {
              repository: gcr.io/ss-infrastructure-public/platform/tools,
              tag: ((vp-tools-container-version)),
            }
          inputs:
          - name: git-deployment
          params: {
            PROJECT: padas-app,
            SERVICE_ACCOUNT_KEY: ((padas-app-cluster-admin)),
            USERNAME: ((creator)),
            VAULT_TOKEN: ((vault-automation-token)),
            VAULT_ADDR: ((vault-address)),
            ELASTIC_URL: ((elastic-url)),
            VCLOUD_SERVICE_URL: ((vcloud-service-url)),
            STATIC_IP: ((static-ip))
          }
          run:
            path: /vulcan-platform-tools/run/run.with.gcloud.service.account.sh
            args:
            - vcloud
            - clusters
            - deploy
            - git-deployment/deployment/components
            - --params-file=git-deployment/deployment/params/((deploy-params-environment)).params.yaml
            - --name=((cluster-name))
            - --workspace=((iaas-workspace))
            - --iaas=gcp
            - --zone=((gcp-compute-zone))
            - --plan=((deployment-plan))
            - --recipe git-deployment/deployment/recipes/((deploy-recipe))
