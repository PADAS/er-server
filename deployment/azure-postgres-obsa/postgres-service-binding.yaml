apiVersion: servicecatalog.k8s.io/v1beta1
kind: ServiceBinding
metadata:
  name: das-postgres-binding
  namespace: <<namespace>>
spec:
  instanceRef:
    name: postgres-instance
  secretName: das-postgres-osba-secret
