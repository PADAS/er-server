apiVersion: v1
kind: Service
metadata:
  namespace: <<infra-namespace>>
  name: alertmanager
spec:
  type: ClusterIP
  ports:
  - port: 9093
    targetPort: 9093
  selector:
    component: alertmanager
