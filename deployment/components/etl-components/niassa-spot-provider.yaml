apiVersion: batch/v1beta1
kind: CronJob
metadata:
  namespace: <<namespace>>
  name: niassa-spot-provider-etl
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          imagePullSecrets:
          - name: gcr-secret
          containers:
          - name: spot-provider-etl
            image: gcr.io/padas-app/etl/spot-provider:latest
            env:
            - name: DAS_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: etl-configmap
                  key: NIASSA_DAS_SENSOR_ENDPOINT
            - name: DAS_TOKEN
              valueFrom:
                configMapKeyRef:
                  name: etl-configmap
                  key: NIASSA_SPOT_PROVIDER_DAS_TOKEN
            - name: SOURCE_GROUP
              valueFrom:
                configMapKeyRef:
                  name: etl-configmap
                  key: NIASSA_SPOT_SOURCE_GROUP
          restartPolicy: Never
