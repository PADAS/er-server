apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  namespace: "${KUBERNETES_NAMESPACE}"
  name: web-admin
spec:
  minAvailable: 1
  selector:
    matchLabels:
      das.component: web-admin
