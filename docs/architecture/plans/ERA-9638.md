ERA-9638 Embed event form builder into admin

* Label the admin menu item: "Event Types and Categories 2.0 (Beta)"
* "View and Edit" instead of just "View".
* Instead of default django view “eye” icon, display a standard external link icon to indicate it'll open in a new tab.
* Clicking the link opens the EFB application in a new tab /admin/form-builder
